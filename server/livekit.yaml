api_key: ${LIVEKIT_API_KEY}
api_secret: ${LIVEKIT_API_SECRET}

signal:
  port: 80

rtc:
  port: 7881
  tcp_port: 7881
  udp_port: 50000-60000
  use_tls: false

external:
  url: "https://murojaah-production.up.railway.app"

turn:
  enabled: false

logging:
  level: info
